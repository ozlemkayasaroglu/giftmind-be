# Netlify _redirects file
# Bu dosyayı frontend projesinin public/ klasörüne _redirects adıyla kaydet

# SPA fallback - tüm route'ları index.html'e yönlendir
/*    /index.html   200

# API calls'ları backend'e yönlendir (eğer proxy kullanıyorsan)
/api/*  https://your-backend-url.com/api/:splat  200

# Specific redirects (opsiyonel)
/home   /   301
